<?xml version="1.0" encoding="UTF-8"?>
<project name="build-all" default="dist">

	<import file="common.xml" />

	<property name="dir-dist" value="dist" />
	<property name="dir-config" value="config" />

	<!--
	<target name="dist">
	<target name="dist" depends="dist-all">
	-->
	<target name="dist" depends="dist-all">
		<delete dir="${dir-dist}" />
		<mkdir dir="${dir-dist}" />

		<zip destfile="${dir-dist}\file-storage.zip">
			<!-- TODO: minden projekthez ant target, hogy src dist -->
			<!-- forrás -->
			<zipfileset dir="${trunk}">
				<exclude name="**\*.class" />
				<exclude name="**\*.jar"  />
				<exclude name="**\*.zip"  />
			</zipfileset>

			<!-- doksik -->
			<zipfileset file="${basedir}\readme.txt" />
		</zip>
	</target>

	<target name="clean-all">
		<delete dir="${dist-all}" />
		<ant dir="${dir-common}" target="clean" />
		<ant dir="${dir-web}" target="clean" />
	</target>

	<target name="dist-all">
		<ant dir="${dir-common}" target="dist" />
		<ant dir="${dir-web}" target="dist" />
		<ant dir="${dir-schemagen}" target="dist" />
	</target>

</project>